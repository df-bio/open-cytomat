[project]
name = "open-cytomat"
version = "0.3.3"
description = ""
readme = "README.md"
requires-python = ">=3.10"
authors = [
    { name = "Niklas Mertsch", email = "niklas.mertsch@stud.uni-goettingen.de" },
    { name = "rsoeldner", email = "r.soeldner@gmail.com" },
    { name = "Thiago Meyer", email = "tiagmey@gmail.com" },
]
dependencies = [
    "pyserial>=3.5,<4",
    "click>=8.1.7,<9",
    "pydantic>=2.9.2,<3",
]

[project.scripts]
open-cytomat = "cytomat.cli:main"

[dependency-groups]
dev = [
    "black>=24.10.0,<25",
    "flake8>=7.1.1,<8",
    "isort>=5.13.2,<6",
    "mypy>=1.4.1,<2",
    "pre-commit>=4.0.1,<5",
    "pytest>=7.4.0,<8",
]

[tool.uv]
default-groups = ["dev"]

[tool.poetry]
name = "open-cytomat"
version = "0.3.3"
description = ""
authors = [
    "Niklas Mertsch <niklas.mertsch@stud.uni-goettingen.de>",
    "rsoeldner <r.soeldner@gmail.com>",
    "Thiago Meyer <tiagmey@gmail.com>",
]
readme = "README.md"
packages = [{ include = "cytomat", from = "src" }]
include = [{ path = "src/cytomat/data/sample_config.json" }]

[tool.poetry.dependencies]
python = "^3.10"
pyserial = "^3.5"
click = "^8.1.7"
pydantic = "^2.9.2"

[tool.poetry.group.dev.dependencies]
black = "^24.10.0"
flake8 = "^7.1.1"
isort = "^5.13.2"
mypy = "^1.4.1"
pre-commit = "^4.0.1"
pytest = "^7.4.0"

[tool.poetry.scripts]
open-cytomat = "cytomat.cli:main"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[[tool.mypy.overrides]]
module = ["serial", "serial.*"]
ignore_missing_imports = true

[tool.isort]
profile = "black"

[tool.pytest.ini_options]
testpaths = [
    "src/cytomat",
    "tests",
]
python_files = [
    "test_*.py",
    "*_test.py",
]
markers = [
    "integration: tests that require physical Cytomat hardware",
]
